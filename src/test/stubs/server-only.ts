// Empty stub for Next.js's `server-only` marker under vitest.
// The package throws when imported from a client bundle at runtime, but in
// unit tests we want to import server modules directly. See vitest.config.ts.
export {};
