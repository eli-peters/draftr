/**
 * Marketing layout — no app shell or navigation.
 * Used for public-facing pages (landing, about, pricing, etc.).
 */
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
